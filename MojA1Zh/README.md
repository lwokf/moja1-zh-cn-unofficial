# Module source

This directory contains the Android/LSPosed module source, generated translation data, build scripts and minimal compatibility inventory.

See the repository root [README](../README.md) and [build instructions](../docs/BUILD.md).

Signing private keys and official Moj A1 application files are intentionally excluded.

