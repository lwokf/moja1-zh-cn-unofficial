Signing keys are created locally when needed and must never be committed, shared or uploaded.

Official release builds use an offline maintainer key. The public certificate SHA-256 fingerprint for release `0.2.1-p1` is:

`EBC446D6B126ADB306BEB0BD40393163BA1EE559B9355AFCF381EB2F2812196A`

The private key is not stored in this repository or in GitHub Actions.
